# Copia questo file come `.env.local` se testi in locale.
# In Vercel NON caricare file .env: usa Settings → Environment Variables (server-only).

# ——— Sirplay ———
SIRPLAY_BASE_URL=https://www.dev.kasirooms.com
SIRPLAY_API_KEY=<INSERIRE_API_KEY_FORNITA_DA_SIRPLAY>

# ——— Sicurezza / Token ———
HMAC_SECRET=<INSERIRE_SEGRETO_CONDIVISO_PER_HMAC>
JWT_SECRET=<INSERIRE_SEGRETO_JWT_ROBUSTO>
JWT_EXPIRES=10m

# ——— CORS ———
# Elenco separato da virgola: dominio prod, preview di Vercel, dev locale
CORS_ORIGIN=https://kasirooms.vercel.app,https://kasirooms-<hash>-vercel.app,http://localhost:3000

# ——— Database (Neon Postgres) - Opzionale ———
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
